# 参与这份项目

> 这份指南很短，因为它想说的只有一件事：**有问题就说，但别指望进度。**

---

## 先说清作者能承诺什么

**能承诺**：会看。

**不能承诺**：回复、修复时间、功能开发。

这个项目是业余产出，没有维护计划（见 [README 的「维护与支持」](README.md#7-维护与支持)）。**所以在你提之前，请先接受这个前提**——不是不欢迎你，是不想让一句"欢迎反馈"变成你等不到的承诺。

---

## 特别欢迎的四类反馈

它们对你可能没用，但对下一个人有用。

### 1. 跑偏报告（最有价值）

这个技能是一段文字加一次 AI 判断，**换个模型就可能不按规矩来了**。而且失败通常不是报错，是"悄悄跑偏"——卷子看着一样工整，实际流程被跳过了。

如果你遇到了，请说清楚：

- **你用的什么模型 / 工具**（这一步很关键，问题是跟着模型走的）
- **该发生什么**：引用技能里的原话（比如"必须先提大纲、等用户确认再出卷"）
- **实际发生了什么**：它跳过了确认？不去读 `specs/`？没标证据等级？

**这一类比"某个功能不好用"重要得多**，因为它是这个项目唯一的验证渠道。

### 2. 文档错误

写错的、自相矛盾的、看不懂的段落。**直接指出来就行，不需要客气。**

如果你发现两处说法打架（比如两节的编号或数字对不上），那更值得说——那类问题最容易漏。

### 3. 实测反馈

真拿它跑过一门课之后的感受。**哪怕结论是"这东西没用"，也是有价值的信息。**

有用的话请带上：什么科目、资料完整度如何（有没有 PPT、课本是不是同一本）、大概刷了多少。

### 4. 你写的规格示例

如果你某门课的 `specs/科目名.md` 调得很好，分享出来能让别人少走弯路。

**但请注意**：不要把某个具体学校、具体老师的偏好当成普遍标准。这个工具的立场是「格式是待确认的假设，不是标准答案」。

---

## 怎么提

**开个 [Issue](https://github.com/Stndyam/exam-forge/issues) 就行。** 不需要 fork，不需要先读什么，不需要打招呼。

没有模板，也**不要求格式**——把上面那几点说清楚就够了。

### 直接改也可以

看得懂就直接改，提 PR。**但不承诺 review 时间**——可能很快，也可能一直搁着。

---

## 这些不用提

- **"能不能加个功能"** —— 可以说，但要知道大概率不会被实现。作者没有维护计划
- **"为什么不支持 XX 平台"** —— 技能包是与平台无关的纯文本，只要你的工具支持技能机制就能装，见 [README 的「需要什么」](README.md#需要什么)
- **催进度** —— 没有进度可催
- **要求删掉某条免责声明** —— 那些声明是有意写的，理由都写在里面

---

## 数据安全

**提交任何东西之前，检查两件事：**

1. **别把你的 `output/` 目录提上来** —— 那里有你的真实错题、真实薄弱章节、以及你对答案的质疑记录
2. **别把你老师的 PPT、课本、真题提上来** —— 那通常有版权，而且和这个仓库无关

这两类东西已经在 `.gitignore` 里排除了，但**手动提交时仍然可能绕过它**。提交前看一眼 `git status`。

**顺带一提**：如果你只想让作者看某段内容（比如一份错题本或一个规格样本），**把内容贴进 issue 文本里就行，不用传文件**——这样至少是你自己选择公开的。

---

## 一句话

**发现问题就开 issue。作者会看，但不保证回复。** 这不是敷衍，是这个项目本来的样子。
